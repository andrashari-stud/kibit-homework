export interface WikiDataResponse {
  entities: {
    Q128518?: {
      type: 'item';
      id: 'Q128518';
      sitelinks: {
        enwiki: {
          site: string;
          title: string;
          badges: [];
          url: string;
        };
      };
    };
  };
  success: number;
}
